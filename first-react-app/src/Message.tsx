// PascalCasing naming convention

function Message() {
    // The below is an example of JSX code: Javascript XML
    const name = 'Daniella';
    if (name)
        return <h1>Hello {name}</h1>;
    return <h1>Hello World</h1>
}

export default Message;